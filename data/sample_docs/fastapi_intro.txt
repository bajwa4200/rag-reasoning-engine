FastAPI is a modern Python web framework for building APIs. It uses type hints
and Pydantic models for request validation. You can run the server with uvicorn
and get automatic OpenAPI documentation at the /docs endpoint.
